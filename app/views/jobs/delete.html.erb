<div class="row">
  <h2 class="subheader">Delete Job</h2>
  <%= form_for(@job, :method => 'delete') do |job| %>
    <p>Are you sure you want to delete the <%= job.object.position %> job listing?</p>
    <%= job.submit("Delete User", :class => "alert button") %>
  <% end %>
</div>
